[project]
name = "practice"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.50.0",
    "slack-bolt>=1.21.0",
]

[tool.setuptools.packages.find]
include = ["ai*", "listeners*"]
